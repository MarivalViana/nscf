spring:
  datasource:
    url: jdbc:sqlserver://paranax1:1433;databaseName=BD_NSCF;encrypt=true;trustServerCertificate=true
    username: sa
    password: car1234
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    database-platform: org.hibernate.dialect.SQLServerDialect
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

server:
  port: 8080

logging:
  level:
    br.scf: DEBUG
    org.springframework.security: DEBUG
